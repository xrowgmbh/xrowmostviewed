How to install this extension1) Alter the body opening-tag and add the data-attribute:<body data-nodeID="{$current_node_id|wash()}">2) Activate the extension xrowmostviewed for example in settings/override/site.ini:[ExtensionSettings]ActiveExtensions[]=xrowmostviewed3) Generate Autoloads for Extensionphp bin/php/ezpgenerateautoloads.php -e4) Clear caches5) Put the block-item "Most Viewed" anywhere into a eZFlow pageRequirements:- jQueryEnsure the following:- FrontendJavascriptList needs to be loaded, otherwise you need to load xrowmostviewed.js anywhere else